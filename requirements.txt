# DouyinBarrage v2 — single virtualenv for the whole pipeline (recorder + ASR + upload).
#   python3 -m venv .venv && .venv/bin/pip install -r requirements.txt

# --- recorder: Douyin danmaku scrape + ffmpeg glue ---
requests>=2.33.1,<3.0
websocket-client>=1.9.0,<2.0
protobuf>=7.34.1,<8.0
proto-plus>=1.27.0,<2.0
pyyaml>=6.0,<7.0

# --- transcription (SenseVoice-Small via FunASR) + ModelScope SDK upload ---
# torch/torchaudio come from the CPU wheel index (recording stations have no GPU).
# The +cpu local version outranks the plain PyPI build, so pip installs the CPU wheels.
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.4
torchaudio>=2.4
funasr>=1.3,<2.0
modelscope>=1.39
